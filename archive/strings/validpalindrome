#reads same foward and backwards
def validpalindrome(self, s):
    cleaned = []

    for c in s:
        if c.isalnum():
            cleaned.append(c.lower()) #cleans string 
    
    return cleaned == cleaned[::-1] #if string is equal to reversed 